// Generated by https://github.com/foxglove/schemas

syntax = "proto3";

import "Vector3.proto";

package foxglove;

// A timestamped pose for an object or reference frame in 3D space
message BoundingBox {
  // 中心点坐标
  foxglove.Vector3 center = 1;
  
  // 三个轴向的半长（x/y/z方向）
  foxglove.Vector3 extents = 2;
  
  // 三个轴向的单位向量（3x3矩阵）
  repeated foxglove.Vector3 axes = 3;  // 改为重复字段，索引0=X轴，1=Y轴，2=Z轴
}
